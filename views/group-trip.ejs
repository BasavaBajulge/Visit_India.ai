<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Group Trip Planner</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .btn-success{
            color: white ;
            background-color: rgb(0, 0, 0);
        }
        .btn-success:hover{
            color: rgb(255, 0, 0) ;
            background-color: rgb(0, 0, 0);
        }
    </style>
</head>
<body>
    <%- include('nav') %>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white text-center">
                        <h2>Plan a Group Trip</h2>
                    </div>
                    <div class="card-body">
                        <form action="/group-trip" method="POST">
                            <div class="mb-3">
                                <label for="myEmail" class="form-label">Your Email</label>
                                <input type="email" class="form-control" id="myEmail" name="myEmail" placeholder="Enter your email" required>
                            </div>

                            <div class="mb-3">
                                <label for="friendsEmails" class="form-label">Friends' Emails</label>
                                <textarea class="form-control" id="friendsEmails" name="friendsEmails" rows="3" placeholder="Enter your friends' emails, separated by commas" required></textarea>
                            </div>

                            <div class="mb-3">
                                <label for="tripPlan" class="form-label">Trip Plan</label>
                                <textarea class="form-control" id="tripPlan" name="tripPlan" rows="4" placeholder="Enter the details of the trip" required></textarea>
                                <button class="btn btn-success"><a href="/ai-trip-planner">Try AI plan </a></button>
                            </div>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="date" class="form-label">Reminder Date</label>
                                    <input type="date" class="form-control" id="date" name="date" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="time" class="form-label">Reminder Time</label>
                                    <input type="time" class="form-control" id="time" name="time" required>
                                </div>
                            </div>

                            <div class="text-center">
                                <button type="submit" class="btn btn-primary w-100">Set Reminder</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
